# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
aiosqlite==0.19.0
alembic==1.13.1
geoalchemy2==0.14.3
shapely==2.0.2
numpy<2.0.0  # Required for shapely compatibility

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
email-validator==2.1.0

# Google Earth Engine & Geospatial (optional for Phase 5)
# Commented out due to binary compilation issues on Render
# earthengine-api==0.1.384
# geemap==0.30.0

# Data Processing
# numpy==1.26.3  # Commented out due to Python 3.13 compatibility issues
# pandas==2.1.4  # Commented out due to Python 3.13 compatibility issues
# matplotlib==3.8.2  # Optional visualization
# Pillow==10.2.0     # Optional image processing

# Reporting (for subsequent phases)
fpdf==1.7.2

# Production Server
gunicorn==21.2.0
